.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Dependencies
/vendor/
node_modules/

# Environment and secrets
.env
.env.local
.env.*.local

# IDE and build
.idea/
.phpunit.result.cache

# Tests: generated reports and artifacts (keep source + fixtures)
# NOTE: directory is "Tests/" (capital T) — git ignore patterns are case-sensitive.
Tests/artifacts/
Tests/artifacts-non-get/
Tests/GET_ENDPOINTS_*.md
Tests/NON_GET_ENDPOINTS_*.md
Tests/*.api.json
Tests/*.sdk.json
Tests/*.raw.json

# Local OpenAPI spec snapshot the validators read, and the per-change check script
/openapi.yaml
/spec_sync_check.*
